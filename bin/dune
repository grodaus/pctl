; pctl binary — cmdliner dispatch over the `cli` library.
; libsystemd is pulled in transitively by `systemctl` so DT_NEEDED is
; satisfied.
(executable
 (name pctl)
 (public_name pctl)
 (package pctl)
 (libraries cmdliner cli systemctl eio eio_main eio.unix))
