; Garbage collection — opportunistic sweep + explicit purge.
;
; Depends on schema + state (DB rows) and unit_store (removes unit files).
; Systemctl is abstracted through the Make functor so tests can substitute
; the In_mem fake, matching Probe.Make.

(library
 (name gc)
 (public_name pctl.gc)
 (libraries schema identity unit_store state systemctl eio eio.unix unix))
