; Plan — pure formatter for the "+ / ~ / = / -" reload summary.
;
; The Systemctl-application logic that used to live in [Plan.Make]
; moved into [Lifecycle.Make] during the phase-5 consolidation; only
; the diff-to-string formatter remains here.
(library
 (name plan)
 (public_name pctl.plan)
 (libraries schema)
 (instrumentation (backend bisect_ppx)))
